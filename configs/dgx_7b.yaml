# METIS DGX 7B Test Configuration

model_name: "Qwen/Qwen2.5-7B-Instruct"
device: "auto"

n_samples_per_prompt: 4
max_new_tokens: 512
temperature: 0.7

dpo_epochs: 1
dpo_learning_rate: 1.0e-6
dpo_batch_size: 4
dpo_beta: 0.1
dpo_max_length: 2048
dpo_gradient_accumulation: 1
gradient_checkpointing: false

lora_r: 16
lora_alpha: 32
lora_dropout: 0.05

n_train_prompts: 5
n_eval_prompts: 2

run_benchmarks: false
